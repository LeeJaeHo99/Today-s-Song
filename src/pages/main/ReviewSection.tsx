import SectionTitle from "@/components/SectionTitle";

export default function ReviewSection(){
    return(
        <section className="review-section">
            <div className="inner">
                <SectionTitle title={`감상평 작성하기`} />
            </div>
        </section>
    );
}